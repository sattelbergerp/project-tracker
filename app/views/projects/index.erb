<%if @projects.count>0 %>
  <div class="list-group">
    <% @projects.each do |project| %>
    <a href="/projects/<%=project.id%>" class="list-group-item"><%=project.name%></a>
    <% end %>
  </div>
<% else %>
  <div class="empty-list-label">You don't have any projects.</div>
<% end %>
